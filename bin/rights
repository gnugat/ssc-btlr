#!/bin/sh

if [ $# -lt 1 ]
then
    echo 'Usage: rights <directory-path>'
    exit 1
else
    path=${1}
fi

echo 'Setting the rights for ' $path
setfacl -R -m u:www-data:rwx -m u:`whoami`:rwx $path
setfacl -dR -m u:www-data:rwx -m u:`whoami`:rwx $path
