# delete

This command allows you to remove an existing Symfony2 project by:

* removing the project directory;
* removing the virtual host;
* removing the log files;
* restarting the apache2 configuration;
* removing the hostname;

## Usage

    delete <absolute-path> <project-name>

The `project-name` will be in most cases the project directory name.
